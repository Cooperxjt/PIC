从 cpc 的所有数据之中抠出来的 400 个用户，其中选取 40 作为最终的测试，360 作为 prior 的训练

split.py: 将每个用户的图片，取出 N 张，分为 train 和 test

csv_total 所有划分出来的相对较多的 400 个人的 csv 文件，prior 用于 prior 模型的训练，test 用于 test 模型的训练

在第二次处理之后，发现有的用户会标注一张图片两次，因此对这个情况进行了处理，只保留了第一次标注的结果。
具体来说， csv_total、prior 与 test 里面的数据都是处理过的
